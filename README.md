# Autonomous B2B RFP Response Architect

## Local Development

Start the local n8n instance and placeholder agents service:

```bash
docker compose up -d
```

n8n will be available at `http://localhost:5678`, and the placeholder agents
service reserves port `8000` for the future FastAPI app.
